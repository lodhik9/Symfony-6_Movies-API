{% extends 'base.html.twig' %}

{% block title %} 
    Movies Page
{% endblock %}

{% block body %}
    <h1 class="bg-blue-500" >Woohoo</h1>
    {# Global Variables #}
    {# {{ _self }}
    {{ author }}
    {{ _charset }} #}
    


    {# {% for movie in movies %}
        <li> {{ movie }} </li>
    {% endfor %} #}

{% endblock %}

{# {% if title %}
    <p>{{title}} is a movie</p>
{% else %}
    <p>No title has set</p>
{% endif %} #}